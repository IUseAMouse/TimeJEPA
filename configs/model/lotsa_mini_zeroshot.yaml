# @package _global_
#
# G5 - FINETUNE ON LOTSA, mini scale. Zero-shot protocol.
#
#   python scripts/train.py --config-name lotsa_mini_zeroshot \
#       trainer.accumulate_grad_batches=2 \
#       '+training.pretrained_encoder_path="checkpoints/timejepa_lotsa_mini/pretrain_True/<ckpt>"'
#
# The decoder is trained only on LOTSA: Monash and Nixtla stay unseen at ALL
# stages, which makes the evaluation comparable to Chronos / Moirai /
# TimesFM / Toto. See `lotsa_tiny_zeroshot` for the full reasoning.
defaults:
  - lotsa_mini
  - _self_

model:
  name: "timejepa_lotsa_mini_zs"

training:
  mode: "finetune"
  finetune_mode: "full_finetune"

data:
  datasets_finetune: null

wandb:
  tags:
    - "mini"
    - "jepa_tst"
    - "experiment"
    - "lotsa"
    - "zeroshot"
