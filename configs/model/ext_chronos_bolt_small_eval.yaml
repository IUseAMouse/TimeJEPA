# @package _global_
# Chronos-Bolt small (48M) through the GIFT harness (2026-09-13, RateIN on third-party
# models). No checkpoint: the model is built from the Hugging Face id and the
# cache is keyed on it (evaluation/chronos_bolt_small/<hf id>/gift<tag>/).
#
#   python scripts/evaluate_gift.py --config-name ext_chronos_bolt_small_eval                       # bare
#   python scripts/evaluate_gift.py --config-name ext_chronos_bolt_small_eval +tta_flip=true       # flip
#   python scripts/evaluate_gift.py --config-name ext_chronos_bolt_small_eval \
#       +tta_flip=true +ratein=mix +ratein_pool=true                                       # the stack
#   +ratein=oracle for the per-config bound (diagnostic, never official)
# Install: uv pip install -e ".[external]"
model:
  name: "chronos_bolt_small"
  builder: "timejepa.evaluation.external:build"
  external:
    kind: "chronos"
    hf_id: "amazon/chronos-bolt-small"
    context_length: 2048
  seq_length: 2048
