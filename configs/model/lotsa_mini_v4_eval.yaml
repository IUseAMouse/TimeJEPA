# @package _global_
# Eval for the corpus-v4 mini checkpoints. Architecture identical to
# lotsa_mini_v3_head8_eval (x8 quantile head); exists for the output namespace
# (v4 and v3 finetunes emit identically-named checkpoints).
defaults:
  - lotsa_mini_v3_head8_eval
  - _self_

model:
  name: "timejepa_lotsa_mini_v4_zs"

wandb:
  tags: ["mini", "jepa_tst", "experiment", "lotsa", "zeroshot", "eval", "v4", "short-series", "robust-scale"]
